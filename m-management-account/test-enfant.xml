<?xml version="1.0"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
    <modelVersion>4.0.0</modelVersion>

    <!-- ============================================================= -->
    <!--                      Project information                      -->
    <!-- ============================================================= -->
    <!-- Parent information -->
    <parent>
        <groupId>com.nanoo.library</groupId>
        <artifactId>m-management-account</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <!-- Module information -->
    <artifactId>m-management-account-web</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>war</packaging>

    <!-- General information -->
    <name>m-management-account-web</name>
    <description>Web module of Microservice Account - project 7 of DA JAVA course from OC</description>

    <!-- ============================================================= -->
    <!--                           Properties                          -->
    <!-- ============================================================= -->
    <properties>
        <application.name>Les amis de l'escalade</application.name>
        <maven.build.timestamp.format>dd/MM/yyyy</maven.build.timestamp.format>
    </properties>

    <!-- ============================================================= -->
    <!--                          Dependencies                         -->
    <!-- ============================================================= -->
    <dependencies>
        <!-- Déclaration des modules -->
        <!--<dependency>
            <groupId>com.nanoo</groupId>
            <artifactId>escalade-business</artifactId>
        </dependency>
        <dependency>
            <groupId>com.nanoo</groupId>
            <artifactId>escalade-model</artifactId>
        </dependency>-->

        <!-- Bibliothèques tierces -->
    </dependencies>

    <!-- ============================================================= -->
    <!--                              Build                            -->
    <!-- ============================================================= -->
    <build>
        <finalName>m-management-account-web</finalName>

        <plugins>
            <!-- Packaging WAR -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <!--<configuration>
                  <webResources>
                    <Directory>${basedir}/src/main/webapp/</Directory>
                    <filtering>true</filtering>
                    <includes>view/common/header.jsp</includes>
                    <includes>view/common/footer.jsp</includes>
                  </webResources>
                </configuration>-->
            </plugin>
        </plugins>
    </build>
</project>
